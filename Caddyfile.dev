:8080 {
	# handle_path automatically strips the matched prefix
    handle_path /be/* {
        reverse_proxy localhost:3000
    }

    # handle serves as a fallback for everything else
    handle {
        reverse_proxy localhost:5173
    }
}